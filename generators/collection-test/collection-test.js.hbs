var {{#camelize}}{{name}}{{/camelize}} = require('models/{{name}}');
var {{#camelize}}{{pluralName}}{{/camelize}} = require('models/{{pluralName}}');

describe('{{#camelize}}{{name}}{{/camelize}}', function(done){
	var self = this;
	beforeEach(function(){
		self.model = new {{#camelize}}{{name}}{{/camelize}}();
		self.collection = new {{#camelize}}{{pluralName}}{{/camelize}}();
	});

	afterEach(function(){
		self.model.dispose();
		self.collection.dispose();
	});
});